// 1. Multiply two numbers to get 62
let num1 = 31;
let num2 = 2;
let multiply = num1 * num2;

// 2. Generate a random integer greater than 0
let random = Math.floor(Math.random() * 100) + 1;

// 3. Calculate the remainder of num3 divided by num4 (mod should be 4)
let num3 = 16;
let num4 = 6;
let mod = num3 % num4;

// 4. Find the highest number in the set (the value should be 20)
let max = Math.max(5, 10, 20, 3, 15);

// Output results (optional for testing)
console.log(`Multiply: ${multiply}`); // 62
console.log(`Random: ${random}`); // Random integer greater than 0
console.log(`Mod: ${mod}`); // 4
console.log(`Max: ${max}`); // 20
